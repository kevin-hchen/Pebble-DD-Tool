# Core
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# Vector search
faiss-cpu>=1.7.4

# LLM + embeddings (primary path)
openai>=1.30

# PDF ingestion
pypdf>=4.2

# UI
streamlit>=1.33

# Encryption at rest (AES-256-GCM + scrypt)
cryptography>=42.0

# Question-set config and memo export
PyYAML>=6.0
reportlab>=4.0

# Local embeddings. NOT optional despite the name: the index in data/index/ was
# built with sentence-transformers, and vectorstore refuses to load an index whose
# embedder does not match. Commented out, `pip install -r requirements.txt` gave a
# working install that then could not open the shipped index.
#
# Pinned exactly, and torch with it, because the embedder name is written into the
# index manifest and a different build can change it. The rest of this file is
# still floor-pinned; the lockfile is a separate task.
sentence-transformers==5.1.2
torch==2.8.0
