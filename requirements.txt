# Core
numpy>=1.24
requests>=2.31
python-dotenv>=1.0

# Vector search
faiss-cpu>=1.7.4

# LLM + embeddings (primary path)
openai>=1.30

# PDF ingestion
pypdf>=4.2

# UI
streamlit>=1.33

# Encryption at rest (AES-256-GCM + scrypt)
cryptography>=42.0

# Question-set config and memo export
PyYAML>=6.0
reportlab>=4.0

# Optional offline embedding fallback (no API key required)
# Install with: pip install -r requirements-offline.txt
# sentence-transformers>=2.7
