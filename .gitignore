.env
.streamlit/secrets.toml
__pycache__/
*.py[cod]
.venv/
venv/

# Generated memos — filenames encode the asset under diligence.
out/

# Fetched trial store and any other sqlite DBs — public data, but there is no
# reason to commit hundreds of MB of registry dumps.
*.db
*.sqlite

data/index/*
data/raw/*
!data/raw/.gitkeep
!data/index/.gitkeep
.pytest_cache/
.DS_Store
*.egg-info/

# Generated memos — can contain live asset names
out/
# Fetched trial stores
*.db
.streamlit/secrets.toml

# Scratch run artifacts
rowut/

# Local working notes — not published
CLAUDE.md
PROMPTS.md
.gstack/

# Built data artifacts — 1.9 GB of fetched public data, rebuildable with
# `python scripts/build_artifact.py`. The manifest and checksums are the record
# of what was built; the bytes belong on a server, not in git.
dist/
